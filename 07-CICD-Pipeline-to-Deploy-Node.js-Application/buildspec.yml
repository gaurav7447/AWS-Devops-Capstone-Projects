version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 18
    commands:
      - echo "Navigating to project folder"
      # Changed from the long project name to the actual folder name
      - cd node-ci-cd-app
      - npm install

  build:
    commands:
      - echo "Build started"
      # If you have tests or a build step, add them here:
      # - npm test 

artifacts:
  # This must match the folder name above
  base-directory: node-ci-cd-app
  files:
    - '**/*'
